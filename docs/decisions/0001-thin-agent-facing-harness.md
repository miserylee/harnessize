# Decision 0001: Thin Agent-Facing Harness

Date: 2026-06-26

## Context

The target user story is intentionally short: users ask their own agent to run
`npx -y harnessize@latest` and follow the guidance.

The expected operator is usually the user's own coding agent, not a human stepping through a
traditional interactive CLI. The CLI should therefore produce guidance that is easy for an agent to
follow, while keeping the user's repository clean.

## Decisions

The root command is the onboarding command:

```sh
npx -y harnessize@latest
```

This should behave as `init`, so the cold-start prompt can stay as short as possible.

Harnessize should inject as little long-lived content into the target repository as practical. The
default adaptation layer should be thin.

The minimum default repository artifact is `AGENTS.md`.

`AGENTS.md` should not contain the full harnessize manual. It should only provide simple local
guidance and point the user's agent back to harnessize for progressive context.

The main long-form manual should live inside the harnessize package and be exposed through:

```sh
npx -y harnessize@latest context [topic]
```

The `context` command is the core progressive-disclosure mechanism. The root context is the stable
entrypoint, and focused topics are selected by the current harnessize version.

The first batch of context topics is intentionally not decided yet. Topics will be discussed and
added one by one.

The harnessize repository itself should be dogfooded. It should be harnessized using harnessize and
should carry the same thin `AGENTS.md` style expected from user repositories.

For this repository's dogfood state, `AGENTS.md` should use the local built CLI:

```sh
node dist/cli.js context
```

User repositories should continue to use the stable npm handoff:

```sh
npx -y harnessize@latest context
```

## Intended Shape

Default `init` behavior should eventually:

1. Detect whether `AGENTS.md` exists.
2. Create a thin `AGENTS.md` when absent.
3. Avoid overwriting existing agent instructions.
4. If an instruction file already exists, guide the agent to merge a small harnessize section.
5. Point the agent toward `harnessize context` for deeper guidance.

An `AGENTS.md` generated by harnessize should primarily answer:

1. This repository uses harnessize for agent workflow guidance.
2. The agent MUST run `npx -y harnessize@latest context` as required session bootstrap before
   repository work.
3. Prefer minimal, repository-respecting changes.
4. Inspect before editing.
5. Preserve user changes.
6. Run relevant verification before reporting completion.
7. If conversation context has been compacted and the harnessize root context is no longer in
   short-term memory, rerun `npx -y harnessize@latest context` before continuing repository work.

The generated `AGENTS.md` should not list specific topics such as `brainstorm` or `grill`. Topics
may change as harnessize evolves, while a user repository's `AGENTS.md` may remain unchanged for a
long time. Root context is the stable handoff point.

## Discussion Record

### Turn 1 - Choose next capability - 2026-06-27 01:35 +08:00

- User signal: The user asked what capability should be added next.
- Agent work: The current CLI behavior was inspected. Root command still returned a scaffold-era
  plan preview instead of performing the intended thin onboarding flow.
- Sources:
  - `src/cli.ts`
  - `src/index.ts`
  - `AGENTS.md`
  - `docs/decisions/0001-thin-agent-facing-harness.md`
- Decisions: The next capability should be real `init` behavior for thin `AGENTS.md` adaptation,
  not another topic.
- Open questions: Exact generated section content still needed refinement.

### Turn 2 - Keep AGENTS.md stable - 2026-06-27 01:35 +08:00

- User signal: The user corrected that generated `AGENTS.md` should point only to root context,
  because topics can change while user repositories may not update their `AGENTS.md`.
- Agent work: The init guidance was revised so generated content depends only on
  `npx -y harnessize@latest context`.
- Sources: Conversation synthesis; no external source.
- Decisions: Generated `AGENTS.md` should not list concrete topic names.
- Open questions: None.

### Turn 3 - Dogfood local dist context - 2026-06-27 03:29 +08:00

- User signal: The user asked the dogfood version to run the local `dist` CLI and update
  `AGENTS.md`.
- Agent work: The local built CLI was verified with `node dist/cli.js context`, then `AGENTS.md` was
  changed to use that command for this repository's root context guidance.
- Sources:
  - `AGENTS.md`
  - `dist/cli.js`
  - `docs/decisions/0001-thin-agent-facing-harness.md`
- Decisions: The harnessize repository dogfoods unreleased context behavior through
  `node dist/cli.js context`. Generated or user-repository guidance should still use
  `npx -y harnessize@latest context`.
- Open questions: None.

### Turn 4 - Require root context bootstrap - 2026-06-27 04:12 +08:00

- User signal: The user said root context should be force-triggered from `AGENTS.md`, and agents
  should rerun it when conversation compaction leaves short-term memory without the harnessize root
  context.
- Agent work: The repository `AGENTS.md` was updated to make local root context a MUST before
  repository work and to require reloading it after compaction when needed. The intended generated
  `AGENTS.md` shape was updated to require `npx -y harnessize@latest context` as session bootstrap.
- Sources:
  - `AGENTS.md`
  - `src/context.ts`
  - `docs/features/context-topics.md`
  - `docs/decisions/0001-thin-agent-facing-harness.md`
- Decisions: `AGENTS.md` remains thin, but root context is mandatory session bootstrap. User
  repositories should use `npx -y harnessize@latest context`; this repository dogfoods with
  `node dist/cli.js context`.
- Open questions: None.

### Turn 5 - Add non-interactive npx flag - 2026-06-27 04:16 +08:00

- User signal: The user said `npx` commands should include `-y` to skip manual confirmation.
- Agent work: User-facing `npx` examples and CLI help were updated to use
  `npx -y harnessize@latest ...`; root context recovery guidance and tests were updated to match.
- Sources:
  - `README.md`
  - `src/cli.ts`
  - `src/context.ts`
  - `test/cli.test.ts`
  - `docs/features/context-topics.md`
  - `docs/decisions/0001-thin-agent-facing-harness.md`
- Decisions: Recommended `npx` invocation should be non-interactive by default, using
  `npx -y harnessize@latest`, so agent cold-start and context recovery do not stall on package
  execution confirmation.
- Open questions: None.

## Non-Goals For The First Implementation Pass

Do not generate a large `.harnessize/` directory by default.

Do not copy full manuals or long playbooks into the user repository.

Do not decide the first topic list prematurely.

Do not implement broad repository modification behavior before the thin adaptation flow is clear.

## Open Questions

What exact section should harnessize add when `AGENTS.md` already exists?

Should `context` output be Markdown only at first, or should `--json` ship in the same version?

Should topic names be stable public API, or can they be experimental before `1.0.0`?

How should dogfood state be represented in this repository before the real `init` command exists?
